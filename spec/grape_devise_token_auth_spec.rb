require 'spec_helper'

describe GrapeDeviseTokenAuth do
  it 'has a version number' do
    expect(GrapeDeviseTokenAuth::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
